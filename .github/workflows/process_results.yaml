name: Process ASV Results
on:
#  pull_request:
#    branches:
#      - main
  schedule:
    - cron: "20 3 * * *"
  workflow_dispatch:

env:
  # Results and other data are stored away from the main branch.
  # This keeps the main branch clean and allows us to clear out
  # objects / history after being built up for long periods of time.
  BRANCH_NAME: shap_2026_07_14
  PYTHON_VERSION: 3.12
  # Workspace root contains the SHAP source checkout. Without
  # this, `python -m asv_runner` would prepend cwd to sys.path and `import
  # shap` could resolve to the unbuilt source tree instead of the installed package.
  PYTHONSAFEPATH: "1"

permissions:
  contents: read

jobs:
  process-results:
    name: Process ASV Results
    runs-on: ubuntu-24.04
    defaults:
      run:
        shell: bash -e {0}
    permissions:
      contents: write
      issues: write
    steps:
      - name: Checkout SHAP
        uses: actions/checkout@v6
        with:
          repository: shap/shap
          fetch-depth: 0

      - name: Checkout asv-runner results branch
        uses: actions/checkout@v6
        with:
          ref: "${{ env.BRANCH_NAME }}"
          path: asv-runner/

      # asv_runner scripts are run from the workflow's own ref (main on schedule,
      # whatever branch you choose on workflow_dispatch), not from whatever
      # stale copy lives on the storage branch.
      - name: Checkout asv-runner code
        uses: actions/checkout@v6
        with:
          path: asv-runner-code/

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "${{ env.PYTHON_VERSION }}"

      - name: Install dependencies
        run: |
          pip install -U pip uv
          uv pip install --system asv pandas pyarrow

      - name: Show environment packages
        run: uv pip freeze

      - name: Prepare ASV workspace
        run: |
          mkdir -p asv_bench
          jq \
            '.repo = ".." | .branches = ["master"] | .benchmark_dir = "../monitoring" | .html_dir = "html" | .results_dir = "results"' \
            asv.conf.json > asv_bench/asv.conf.json

      - name: Process and push
        run: |
          PYTHONPATH=asv-runner-code python -m asv_runner process \
            --storage-dir=asv-runner \
            --asv-dir=asv_bench \
            --branch=$BRANCH_NAME
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
